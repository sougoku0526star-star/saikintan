# スコープ方針（彩金譚）

コンセプトは「留学中の食生活を、ゆるく正してくれる相棒」。
記録アプリではなく、**行動修正の相棒**。検証したい仮説は「“ゆるく正す相棒”の需要」。

## 確定した設計原則（全実装の判断基準）

- **支出は精密・栄養はゆるく**。支出の数値（金額・予算比・着地予測）はごはんくんが言及してよい。
  栄養は数値（g / kcal / % / mg）を一切出さず、「少なめ」「いい感じ」「多め」等の方向感の言葉のみ。
- **精度は競争軸にしない**。栄養データの誤差は方向感への丸めで戦略的に無効化する。
- ごはんくんは絶対に責めない。身体・体重への言及は全面禁止。
- アドバイス（行動提案）は常に1個だけ。
- AIの判断は、アプリ側ロジックで確定した事実の「翻訳」に限定する。AIに計算・判定をさせない。
- 広告＋Exitモデルのため、1ユーザーあたりのAPI原価を常に意識する。

## スコープ凍結（当面、追加開発しない）

以下は「ゆるく正す相棒」の仮説検証に対して優先度が低いため、**当面は投資しない**（既存は残してよい）。

- フレンド・メッセージ・ユーザー検索まわりの機能拡張
- 栄養データの精密化（g単位の精度向上、食品DB拡充の深掘り）
- 一食ごとの自由チャット機能（週次レター＋一食コメントで行動修正効果の大半は取れる想定。v2で再検討）

新機能を足したくなったら、まずこの原則・凍結リストと照らし合わせること。
